import React from "react";

const Info = () => (
    <div>
        <h2 style={{textAlign: "left"}}>Weather App</h2>
        <h3 style={{textAlign: "left"}}>Enter the name of the city</h3>
        <h3 style={{textAlign: "left"}}>City name can only contain Latin letters and numbers</h3>
    </div>
);

export default Info;