import React from "react";

const Info = () => (
    <div>
        <h2 style={{textAlign: "center"}}>Enter the name of the city</h2>
        <h3 style={{textAlign: "center"}}>City name can only contain Latin letters and numbers</h3>
    </div>
)

export default Info;